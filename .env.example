# Copy to `.env` and adjust after deployment.
#
# Django / API origin (scheme + host, NO trailing slash).
# If you see "socket hang up" in the Vite terminal, the tunnel or host below is
# unreachable — use local Django instead, e.g. http://127.0.0.1:8000
VITE_API_BASE=https://captivative-stalkable-india.ngrok-free.dev

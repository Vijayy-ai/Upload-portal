# Copy to `.env` and adjust after deployment.
#
# Django / API origin (scheme + host, NO trailing slash).
# If you see "socket hang up" in the Vite terminal,# Copy this to .env (or configure in Vercel)
#
# Production: API_URL=https://snoozeit.publicvm.com
# Local: API_URL=http://localhost:8000

API_URL=https://captivative-stalkable-india.ngrok-free.dev
